/*
import '../timeline/entry.dart';

class Principal {

  int id;
  int annee;
  String affair;
  String pays;

  Principal(
      this.id,
      this.annee,
      this.affair,
      this.pays,
      );

  TimelineEntry toTimelineEntry() {
    TimelineEntry timelineEntry = TimelineEntry();
    timelineEntry.type = TimelineEntryType.incident;
    timelineEntry.start = annee.toDouble as double; // Assuming 'annee' is a field of Principal
    timelineEntry.name = affair; // Assuming 'affair' is a field of Principal
    timelineEntry.end = timelineEntry.start;
    return timelineEntry;
  }


}*/
